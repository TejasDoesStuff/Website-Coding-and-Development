import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Connext - Page Name",
    description: "Specific description for this page",
};

export default function ServerPage() {
    return (
        <div>
            {/* ... component content ... */}
        </div>
    );
} 